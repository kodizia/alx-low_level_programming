#include "main.h"
#include <string.h>

/**
 * int _strlen - function that returns the length of a string
 * @*s: pointer to the string
 * Return: strlen;
 */
int _strlen(char *s)
{
return (strlen(s));
}
