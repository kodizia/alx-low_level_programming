#include "main.h"
#include <ctype.h>
/**
 * mul - function that multiplies two integers
 * @a: integer that are to be multiplied
 * @b: integer to be multiplied
 * Return: a * b;
 */
int mul(int a, int b)
{
return (a * b);
}
